apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-controller
spec:
  template:
    spec:
      serviceAccountName: argo
      containers:
        - name: workflow-controller
          args:
          - --configmap
          - workflow-controller-configmap-i1
